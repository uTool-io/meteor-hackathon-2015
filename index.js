var path = __dirname,
    archive = path + '/archive/',
    dist = path + '/dist/',
    src = path + '/src/',
    test = path + '/test/',
    wiki = path + '/wiki/';

module.exports = {
    path: path,
    archive : archive,
    dist: dist,
    src: src,
    test: test,
    wiki: wiki
};